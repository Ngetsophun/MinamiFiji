

// class ThemeManager with ChangeNotifier {
//   ThemeMode _themeMode = ThemeMode.light;
//   get ThemeMode => _themeMode;

//   ToggleButtonsTheme(bool isDart) {
//     _themeMode = isDart ? ThemeMode.dart : ThemeMode.light;
//   }
// }
